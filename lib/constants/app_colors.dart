import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryLight = Color(0xFF26C6DA); // cyan.shade400
  static const Color primaryDark = Color(0xFF00ACC1);  // cyan.shade600

  // Text colors
  static const Color white = Colors.white;
  static const Color grey = Color(0xFF757575);

  // Error colors
  static const Color error = Color(0xFFE53935);
  static const Color errorDark = Color(0xFFB71C1C);

  // Shadows
  static Color primaryShadow = Colors.cyan.withOpacity(0.3);
}
